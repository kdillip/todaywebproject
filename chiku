pipeline {
    agent any 
    
    stages {
        stage("one1") {
            steps {
                sh 'touch file2'
                sh 'lsmem'
                sh 'lscpu'
                sh 'ls -la'
            }
        }
    }
}
