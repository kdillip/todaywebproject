pipeline {
    agent any 
    
    stages {
        stage("one") {
            steps {
                sh 'touch file2'
                sh 'lsmem'
                sh 'lscpu'
            }
        }
    }
}
